# --- Node & general ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json*
yarn.lock*
pnpm-lock.yaml*
bun.lockb*

# If you prefer committing a lockfile, remove the relevant line above.

# --- Next.js ---
.next/
.next/static/
.next/cache/
out/                 # next export output
# DO NOT ignore next-env.d.ts (keep it tracked)

# --- Environment variables ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Logs & coverage ---
logs/
*.log
*.log.*
coverage/
.nyc_output/
lerna-debug.log*

# --- TypeScript build output (only if you emit to ./dist) ---
dist/
tsbuildinfo.tsbuildinfo
*.tsbuildinfo

# --- Testing (Jest/Vitest/Playwright) ---
jest-html-reporters-*
test-results/
playwright-report/
.playwright/
.vitest/
*.snap
*.swp

# --- OS / Editor junk ---
.DS_Store
Thumbs.db
Icon?
ehthumbs.db
*.seed
*.pid
*.pid.lock

# VS Code
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

# WebStorm / IntelliJ
.idea/
*.iml

# --- Docker / local tooling ---
.docker/
docker-compose.override.yml

# --- Vercel ---
.vercel/

# --- Turborepo / Turbo cache (if using) ---
.turbo/
turbo-run-*

# --- Storybook (if using) ---
.storybook-out/
storybook-static/

# --- Misc build artifacts ---
*.local
*.cache
.cache/
.tmp/
tmp/
